default:
  autopilot: false
  confirm_irreversible: true
  max_file_size_mb: 100
  allowed_shell_commands:
    - "ls"
    - "cat"
    - "echo"
    - "python"
    - "pip"
    - "git"
  blocked_shell_commands:
    - "rm -rf /"
    - "format"
    - "shutdown"
    - "reboot"
