# Zenith-OS — Radical Innovation & Anti-Mimicry Directive
> Rule Status: UNCOMPROMISING ENFORCEMENT. Rejects generic industry boilerplate.

## 1. BAN ON INDUSTRY BOILERPLATE (ANTI-LANGCHAIN)
* **No Vector-RAG Obsession**: Do not implement standard "chunk-and-retrieve" vector database patterns for Soft Memory. Zenith-OS uses the `DreamController` for asynchronous semantic compilation and structural tensor comparisons.
* **No Agentic Spaghetti Frameworks**: You are forbidden from mimicking LangChain's chains, AutoGen's conversational wrappers, or CrewAI's task-binding abstractions. Zenith-OS is an OPERATING SYSTEM, not an application workflow. All agent communications must obey the asynchronous, non-blocking `Token Bucket` permission queue.

## 2. FIRST-PRINCIPLES STRUCTURAL THINKING
* When building any core component (e.g., `ZeroErrorFilter`, `UnitStandardizer`), your implementation must rely on hard mathematical verifications, dimensional normalization (SI standard transformation), and deterministic state machines. 
* Do not encapsulate logic into "blackbox prompt chains" where an explicit Python function or a registry lookup can solve the boundary condition deterministically.

## 3. MASTER PLAN ALIGNMENT ALWAYS
* Do not look at open-source project standards on GitHub to decide "best practices." The ONLY source of truth for architecture boundaries is the updated `ZENITH_FINAL_MASTER_PLAN.md`. If a standard framework design patterns conflicts with Zenith's memory hierarchy or asynchronous signal handling, Zenith principles win unconditionally.